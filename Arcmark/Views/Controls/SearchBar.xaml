<UserControl x:Class="Arcmark.Views.Controls.SearchBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="36" d:DesignWidth="340">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Views/Templates/ThemeResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVis" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="#14000000"
            CornerRadius="8"
            Height="34"
            Padding="10,0,6,0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Search icon -->
            <TextBlock Grid.Column="0"
                       Text="🔍"
                       FontSize="13"
                       VerticalAlignment="Center"
                       Margin="0,0,6,0"
                       Opacity="0.5" />

            <!-- Text input -->
            <TextBox Grid.Column="1"
                     x:Name="SearchTextBox"
                     Style="{StaticResource CleanTextBoxStyle}"
                     Text="{Binding SearchQuery, RelativeSource={RelativeSource AncestorType=UserControl},
                            Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"
                     FontSize="13"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="0"
                     Foreground="{StaticResource DarkGrayBrush}"
                     CaretBrush="{StaticResource DarkGrayBrush}"
                     KeyDown="OnKeyDown" />

            <!-- Placeholder text -->
            <TextBlock Grid.Column="1"
                       Text="Search in workspace"
                       Foreground="{StaticResource DarkGrayBrush}"
                       Opacity="0.4"
                       FontSize="13"
                       VerticalAlignment="Center"
                       IsHitTestVisible="False">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SearchQuery,
                                         RelativeSource={RelativeSource AncestorType=UserControl}}"
                                         Value="">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- Clear button -->
            <Button Grid.Column="2"
                    x:Name="ClearButton"
                    Width="20" Height="20"
                    Padding="0"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    VerticalAlignment="Center"
                    Click="OnClearClicked">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasText,
                                         RelativeSource={RelativeSource AncestorType=UserControl}}"
                                         Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="Transparent" CornerRadius="10">
                            <TextBlock Text="✕" FontSize="11"
                                       Foreground="{StaticResource DarkGrayBrush}"
                                       Opacity="0.6"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background">
                                    <Setter.Value><SolidColorBrush Color="#1A000000" /></Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</UserControl>
